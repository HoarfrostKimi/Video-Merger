# 视频合并 App — 加固改造 v1.1 QA 独立验证报告

**验证人**：Edward（QA 工程师 / software-qa-engineer）
**验证日期**：2026-07-06
**对象 commit**：`460af6c`（refactor: 安全/性能/代码质量加固）
**验证性质**：第二层独立验证（不重复工程师自验命令，额外补充新模块测试 + 静态取证）

---

## 1. 验证结论

**IS_PASS 结论与工程师一致：YES（加固生效，全部通过）。**

- 工程师自验：IS_PASS=YES、tsc 0 error、vitest 61 passed、build 成功。
- 我的独立复跑：**vitest 85 passed（在原 61 基础上新增 24）、tsc 双配置 0 error**。
- 加固项（A1 sandbox / A2 CSP / A3 userData 回归 / A4 IPC 校验 / B1 异步扫描 / B2 真实时长 / C1–C7）均经**静态取证 + 测试**证实真实落地，非纸面声明。

**路由判定（Smart Routing）：NoOne（全部通过，无源码 Bug，无测试代码 Bug 需他人修复）。**

---

## 2. 独立测试结果（真实复跑，非采信工程师结论）

| 命令 | 结果 | 说明 |
|------|------|------|
| `npx vitest run` | ✅ 85 passed / 0 failed（8 个测试文件） | 原 61 + 新增 24（security 15 / userData 6 / mergeProgress 3） |
| `npx tsc -p tsconfig.node.json --noEmit` | ✅ 0 error | 覆盖 main / preload / shared |
| `npx tsc -p tsconfig.web.json --noEmit` | ✅ 0 error | 覆盖 renderer / shared |

> 注：`tsc -p` 配合 `composite:true` 在本环境可正常带 `--noEmit` 执行，未报 TS6379。strict 模式下源码与共享层编译全绿，证明 C1（strict 引爆点）已一次性修完。

---

## 3. 静态加固取证（grep / Read 证据）

| 加固项 | 证据（文件:行） | 结论 |
|--------|----------------|------|
| **A1 sandbox:true** | `src/main/index.ts:206` `sandbox: true` | 真实落地；preload 仅 `contextBridge`+`ipcRenderer`，ffmpeg 在主进程 spawn 不受影响 |
| **A5 收敛 electronAPI** | `src/preload/index.ts:21` 仅注释提及；全仓 grep `electronAPI` 无运行期暴露 | 旧 `@electron-toolkit/preload` 完整 `electronAPI` 已移除，仅暴露受限 `api` |
| **A3 移除 setPath + 迁移最先执行** | `src/main/index.ts` grep `setPath` 无命中（仅 `userData.ts:30` 用于 env 覆盖）；`index.ts:369` `await migrateLegacyUserData()` 为 `whenReady` 首个调用 | 回归 Electron 默认 userData；迁移优先于首次 `getPath` |
| **B1 扫描异步** | `src/main/index.ts:5` `import { promises as fsPromises }`；`:80/:88/:119/:136` 全部 `fsPromises.readdir/readdir`(withFileTypes) + `stat` 递归 | 同步 `readdirSync` 已消除 |
| **C6 修复 Promise 反模式** | 全仓 grep `new Promise(async` 无命中 | `mergeVideos` 改为 `async` 函数 + 内层普通 Promise，`run().catch(reject)` 模式 |
| **A4 IPC 白名单守卫** | `src/main/security.ts:80` `safeHandle`；`:84` `ALLOWED_IPC_CHANNELS.has(channel)` 拒绝未授权；`index.ts` 10 个 handler 全部经 `safeHandle`（`:239–360`）+ `validateFolderPath/validateOutputPath` 入参校验 | 白名单 + 路径越界校验真实生效 |
| **A2 CSP 防御纵深** | `src/main/index.ts:222` `if (!is.dev)` 下 `setContentSecurityPolicy(CSP_POLICY)`（带运行时能力探测，dev 跳过以免破坏 HMR）；`src/renderer/index.html:6-10` `<meta http-equiv="Content-Security-Policy">` | 生产包 + HTML meta 双保险 |
| **C7 测试真实化** | 5 个测试文件均 `await import('../src/main/index' | '../src/main/ffmpeg' | '../src/preload')`，无函数体镜像副本 | 验证的是真实源码，非副本（零覆盖问题已解决） |

---

## 4. 新增 / 修改的测试文件

| 文件 | 类型 | 覆盖点 |
|------|------|--------|
| `tests/security.test.ts` | **新增** | `validateFolderPath`/`validateOutputPath`：空串、非字符串、包含 `..` 穿越且解析到不存在目录、受限系统目录越界、合法目录通过、父目录为文件拒绝、父目录可创建通过；`safeHandle`：非法 channel 不注册、合法 channel 透传返回值、handler 抛错被包裹为 `{success:false,message}`（共 15 用例） |
| `tests/userData.test.ts` | **新增** | `resolveUserDataPath`：env 覆盖 / 默认回退；`migrateLegacyUserData`：无旧配置安全返回、旧→新复制并删除旧、新已存在则保留新并清理旧、env 覆盖写入指定位（共 6 用例） |
| `tests/mergeProgress.test.ts` | **新增** | 实证进度用「各文件真实时长求和」：双文件 100s+1000s，第 550s 进度≈50% 且 <99.9%（证明非旧「首文件码率×大小」）；锁定文件边界：全不可访问→拒绝「全部占用」、部分可访问→跳过坏文件继续（共 3 用例） |
| 5 个既有测试文件 | 未改（仅复跑验证） | 确认仍 `import` 真实源码且无副本残留 |

> 新增测试均 mock `electron` 与 `child_process`/`ffmpeg`，复用 `tests/mocks/ffmpeg.ts` 桩，未触碰任何业务源码。

---

## 5. 发现的问题与路由判定

- **源码 Bug**：未发现。校验逻辑、迁移边界、sandbox 写法、strict 修复均正确。
- **测试代码 Bug**：未发现需他人修复项（新增测试一次性通过）。
- **路由判定：NoOne**。

**良性噪声（非 Bug，仅记录）**：`configAndUtils.test.ts` 与新增 `userData.test.ts` 均在模块加载时触发 `migrateLegacyUserData()`，二者共享同一旧路径（`项目/user-data/config.json`），并发下可能出现一次 `ENOENT unlink` 告警——该异常被 `migrateLegacyUserData` 的 best-effort `try/catch` 吞掉（设计如此，不阻断启动），不影响任何断言。验证后已确认 `user-data/config.json` 不存在（项目保持干净）。

---

## 6. 遗留风险（需人工/运行期确认）

1. **sandbox 运行期桌面验证（最高优先）**：`sandbox:true` 的编译/静态正确性已确认，但**渲染进程在真实沙箱下的运行时行为（窗口加载、contextBridge 注入、`window.api` 可用性、关键 IPC 往返、FFmpeg spawn、控制台无 `Blocked … in sandboxed renderer` 报错）需人工在打包后桌面环境跑一遍**。本环境无法在 headless 下验证渲染进程沙箱运行时。若回归，按设计回退为 `sandbox:false` 但保留 A2/A4/A5 基线。
2. **超时保护（30min）与输出已存在备份**：`ffmpeg.ts` 的 `MERGE_TIMEOUT_MS` 超时保护、输出已存在自动 `_backup.mp4` 逻辑（`:157–162`、`:212–226`）经静态 + strict 编译确认正确，但**功能级单测需真实 ffmpeg 产出临时文件**，本环境以 mock 无法覆盖，列为已知未单测项。
3. **E2E / 渲染逻辑测试**：项目仍无 `Home.tsx` 渲染测试与端到端测试（属原 W5 范围外，本次未纳入）。

---

**最终状态**：✅ 加固验证通过，85/85 测试通过，0 源码错误，新增 24 个针对新模块（security/userData）与进度真实性的测试，路由 NoOne。
